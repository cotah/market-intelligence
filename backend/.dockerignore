# O que NAO copiar para a imagem Docker (evita vazar segredos e quebrar o venv).

# Segredos
.env
.env.local
*.env

# Ambiente virtual local (Windows) - a imagem cria o seu proprio
.venv/

# Caches Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Git / meta
.git/
.gitignore
.dockerignore
Dockerfile
